#include "monty.h"
/**
 * f_stack - This print the top
 * @head: the stack head
 * @counter: total line_number
 * Return: nothing is to return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
